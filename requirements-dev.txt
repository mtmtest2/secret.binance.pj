# ---------------------------------------------------------------------------
# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/ -q
# ---------------------------------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.24.0
# Required by starlette.testclient.TestClient - used to exercise the FastAPI
# panel's routes (module_f_panel.web_app) end-to-end in tests.
httpx>=0.27.0
